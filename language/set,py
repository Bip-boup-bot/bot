from namedb import add, search, edit
def set(user_id: int, language: str):
  if search(str(user_id), categorie="language") is True:
    return edit(str(user_id), language, categorie="language")
  return add(str(user_id), language, categorie="language")
